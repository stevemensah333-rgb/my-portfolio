<section id="about" class="about" aria-labelledby="about-title">
  <div class="page-shell about-layout">
    <header>
      <p class="technical-label">About</p>
      <h2 id="about-title">Curious about the parts that break.</h2>
    </header>

    <div class="about-copy">
      <p>I'm Stephen, a Computer Science student who gets curious when software behaves in ways it shouldn't.</p>
      <p>
        AI systems are especially interesting to me because getting a model to respond is usually the easy part.
        The harder problems start when real inputs arrive, assumptions break and the output has to fit into an actual product.
      </p>
    </div>
  </div>
</section>

<style>
  .about {
    padding-block: clamp(var(--space-9), 12vw, var(--space-10));
    border-top: var(--border-default);
    scroll-margin-top: 5rem;
  }

  .about-layout {
    display: grid;
    gap: var(--space-7);
  }

  header p,
  header h2 {
    margin: 0;
  }

  header h2 {
    max-width: 12ch;
    margin-top: var(--space-3);
    font-size: var(--text-h2);
    font-weight: 600;
    letter-spacing: -0.035em;
  }

  .about-copy {
    max-width: var(--width-reading);
  }

  .about-copy p {
    margin-bottom: var(--space-5);
    color: var(--color-text-secondary);
    font-size: var(--text-body-large);
    line-height: 1.6;
  }

  .about-copy p:first-child {
    color: var(--color-text);
  }

  .about-copy p:last-child {
    margin-bottom: 0;
  }

  @media (min-width: 48rem) {
    .about-layout {
      grid-template-columns: minmax(13rem, 4fr) minmax(0, 8fr);
      gap: var(--space-8);
      align-items: start;
    }
  }
</style>
